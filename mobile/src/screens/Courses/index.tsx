import React from 'react';
import { View } from 'react-native';

import Icon from 'react-native-vector-icons/Feather';

const Courses: React.FC = () => (
  <View style={{ flex: 1 }}>
    <Icon name="home" color="#f00" size={60} />
  </View>
);

export default Courses;
